---
title: "Unix: an operating system with 50+ years of <br>significance in scientific computing"
keywords: "Productivity Tools"
date: "2026-09-02"
format:
  revealjs:
    theme: solarized
---
## Before we begin ... partial recap of Aug 31

* When designing a table for data organization, which is most important?

    - choice of columns
    - choice of rows
    - options for cell contents

* When different groups wish to combine data from separately created tables, 
how should they proceed?

* [PRIMED](https://primedconsortium.org/research/data) is a relevant resource 

## Another point about table design

* It can be useful to think of rows as "replicates" -- observations on
sets of features (columns) that are "replicated" on different "study subjects"
* In our case, individual students were subjects, and the groups that were
formed are a higher-level "study unit"
* We want to be able to distinguish which responses come from different
units at different levels
* need a group identifier and a subject identifier
* subject may have multiple observations

## Please fill out second form: https://tinyurl.com/bst260form2

This version does not have free text responses on tool usage, etc.

We do need accurate statement of "github handle"

## Emerging role of github in content management for the course{.smaller}

I asked students to invite the teaching team as collaborators.  We
will change this arrangement, but it is of interest to be able to
assess this strategy.  Code was written to use the GitHub API to
check created repos and contents.
```
> github_private_repo_collaborators([handle], "bst260")
       login        id type role_name site_admin admin maintain push triage pull
1     vjcitn   4624066 User     write      FALSE FALSE    FALSE TRUE   TRUE TRUE
2 QianyuFan8  88663047 User     write      FALSE FALSE    FALSE TRUE   TRUE TRUE
3  [handle]   [id    ] User     admin      FALSE  TRUE     TRUE TRUE   TRUE TRUE
> dir("R")
[1] "github_private_repo_collaborators.R" "github_repo_has_file.R"              "github_repo_markdown_headers.R"     
> source("R/github_repo_markdown_headers.R")
> args(github_repo_markdown_headers)
function (org, repo, folder, file, token = NULL) 
NULL
> github_repo_markdown_headers([handle], "bst260", "task-01", "README.md")
[1] "Overview"
```
Two points:

- default assignment of 'write' privilege to collaborators should probably not be taken
- the API permits retrieval of content, which will be used for grading


## A little history of unix and "commodity" computing

* We take for granted the notion that we can "start the computer" and "type in commands" and
get essentially immediate responses for many tasks
* The origins of this capability are historically complex; let's have a little
background

## Antitrust (facts from sonnet and gemini){.smaller}

* True or false: Because of AT&T's 1956 antitrust consent decree, the company was barred from competing in the computer business, so it could not sell Unix as a commercial product. Instead it licensed the source tape to universities for a nominal fee (little more than the cost of media and shipping).

* True: To resolve an antitrust lawsuit (United States v. Western Electric Co.), AT&T signed a consent decree restricting the Bell System to regulated "common-carrier telecommunications services" and related equipment manufacturing. The decree legally barred AT&T from entering other commercial markets, including data processing and computer software.


## "Shipping costs"{.smaller}

* Licensing Unix "As-Is": Because AT&T could not market Unix as a commercial software product, Bell Labs provided the source code to universities, research labs, and government agencies under an educational license. The fee was nominal—typically around $150 to $300—just enough to cover magnetic tapes, printed documentation, and handling. AT&T explicitly provided no technical support, warranty, or bug fixes.
* The Impact: This policy inadvertently sparked a computing revolution. Because universities received full source code and had to maintain the OS themselves, a generation of students and researchers studied, debugged, and extended Unix. This culture gave rise to John Lions' famous [Commentary on UNIX 6th Edition](https://en.wikipedia.org/wiki/A_Commentary_on_the_UNIX_Operating_System), the Berkeley Software Distribution (BSD), and the modern open-source movement.
When AT&T was broken up in 1984 and released from the 1956 decree, the company immediately sought to commercialize Unix (System V) and enforce strict, expensive licensing, igniting the famous "Unix Wars."

## DOS vs UNIX{.smaller}

Try a prompt like

```
Can you provide some facts and interpretations, and discuss a bit how 
DOS came about -- why unix could not be relevant for microcomputers in the evolution of microsoft.
```

to learn about CP/M, QDOS, and steps towards windows powershell.  You will not be responsible
for these facts but they are interesting: UNIX was intrinsically multiuser, share the hardware;
DOS addressed a hardware limitation focusing on single-user processing for consumer-grade chips

Conclusion: AT&T was [broken up](https://en.wikipedia.org/wiki/Modification_of_Final_Judgment)

Maybe search for "how did the MFJ issued for AT&T affect its ability to continue bell 
labs scientific research and operating system development"

## Some future targets for the class

* We will establish accounts on a linux system through GitHub code spaces (verify that you can start one from your GitHub IDE)
* We will create portable software tools in a compiled language, possibly C and C++
* We will learn how to use debuggers and profilers to identify errors and performance deficiencies

## Naming conventions for file collections

* Name your files in a way that is related to their contents and specifies how they relate to other files. 

* The [Smithsonian Data Management Best Practices](https://library.si.edu/sites/default/files/tutorial/pdf/filenamingorganizing20180227.pdf) has "Five precepts of file naming and organization"

* Any folder worth creating deserves a README.md inside the folder explaining why it was
made and what its intended contents are
--- 

## Five precepts of file naming and organization{.smaller}

* Have a distinctive name that describes the content.
* Follow a consistent pattern that fosters convenient references to files
to simplify software that operates on them
* Organize files into directories that follow a consistent pattern.
* Avoid repetition of semantic elements among file and directory names.
    - What could that mean?
* Have a file extension that matches the file format (no changing extensions!)
    - jpg and jpeg are suffixes for a fixed image format
    - sometimes csv is used as a suffix for tsv information!  and *vice versa*
    - .txt conveys very little information
* markdown and json are formats that you should be comfortable with

For specific recommendations we recommend you follow The [Tidyverse Style Guide](https://style.tidyverse.org/)

--- 

## The System{.smaller}

- Instead of clicking, dragging, and dropping to organize our files and folders, we will be typing Unix commands into the terminal. 

- The way we do this is similar to how we type commands into the R console, but instead of generating plots and statistical summaries, we will be organizing files on our system.

- The file system is hierarchical, you create directories (folders) in other folders

- When you log in to a Unix computer you are in your **home** directory

- the command line has a *prompt* that is configurable and is printed on screen to let you know that the terminal is ready for input

- **Environment variables** control your computing environment and many are customizable by users 

- LLMs are useful tools for finding commands and getting help navigating through your first exposure to Unix and the ideas it is based on

---

## The terminal

- The terminal is integrated into Mac and Linux systems, but Windows users will have to install an emulator. 

- Once you have a terminal open, you can start typing commands. 

- You should see a blinking cursor at the spot where what you type will show up. This position is called the command line. 

---

## The filesystem 

<!-- fixme?  make this local? -->

![]({{< var dsbook1 >}}productivity/img//unix/filesystem.png)

---

## The home directory{visibility="hidden"}


::: {layout-ncol=2}

![]({{< var dsbook1 >}}productivity/img//windows-screenshots/VirtualBox_Windows-7-Enterprise_23_03_2018_14_53_13.png) 

![]({{< var dsbook1 >}}productivity/img//mac-screenshots/Screen-Shot-2018-04-13-at-4.34.01-PM.png) 
:::


--- 

## Windows{visibility="hidden"}

The structure on Windows looks something like this:

![]({{< var dsbook1 >}}productivity/img//unix/windows-filesystem-from-root.png)

---
 
## Mac{visibility="hidden"}
 
 And on MacOS something like this:

![]({{< var dsbook1 >}}productivity/img//unix/mac-filesystem-from-root.png)

---

## Files and permissions

* within each directory you can create files and other directories

* files whose names start with a period `.` are *hidden* by default; use `ls -a` to view them

* files have permissions: use `ls -lh` to view details

```{r}
#| echo: true
#| eval: false
#| code-line-numbers: false

drwxr-xr-x    4 robert  staff   128B Aug  8 10:03 bin
-rw-r--r--@   1 robert  staff    20M Jun 24 09:11 boots.png
-rw-r--r--@   1 robert  staff    26M Jun 23 15:23 boots1.pdf
```

* you can **r**ead, **w**rite, or e**x**ecute files and control who can see or use them by changing permissions

## Working directory

- The working directory is the directly you are currently *in*. 

- Commands you issue will generally take effect in this directory

- You can see your working directory using the Unix command `pwd`

- You can list the contents of the directory (files and other directories) using `ls`

- You can find out about how `ls` works by issuing the command: `man ls`


---

## Paths 

- The string returned by `pwd` command is the *full path* to the working directory.

- The full path to your home directory is stored in an *environment* variable. 

- You can see it like this `echo $HOME`


---

## Paths {.smaller}

- In Unix, we use the shorthand `~` as a nickname for your home directory

  - Example: the full path for *docs* (image in prev. slides) can be written like this `~/docs`.

- A common default for the prompt is to show the path to the working directory (that path might be long, but you can change it)

- Let's open a terminal window and see what the prompt is.

- you have an environment variable called `PATH` which you can display using `echo`

  - that variable is a set of individual paths, separated by `:` (colon) and it tells your Unix shell where to look for commands

  - you can modify that path, so that you can have it look for commands in other, non-standard places (eg make your own `bin` directory)

---

## Unix commands

- `ls`: Listing directory content

- `mkdir` and `rmdir`: make and remove a directory

- `cd`: navigating the filesystem by changing directories

- `pwd`: see your workding directory

- `mv`: moving files

- `cp`: copying files

- `rm`: removing files

-  `more` and `less`: display the contents of a file

---

## Autocomplete

- In Unix you can auto-complete by hitting tab. 

- This means that we can type `cd d` then hit tab. 

- Unix will either auto-complete if `docs` is the only directory/file starting with `d` or show you the options. 

- Try it out! Using Unix without auto-complete will make it unbearable.



## Text editors

Command-line text editors are essential tools, especially for system administrators, developers, and other users who frequently work in a terminal environment. Here are some of the most popular command-line text editors:

* Nano 
* Pico 
* Vi or Vim 
* Emacs 


## Other useful commands

* `curl` - download data from the internet.

* `tar` - archive files and directories into one file.

* `gzip`, `zstd` - and other compression tools - make big files small

* `ssh` - connect to another computer.

* `find` - search for files by filename in your system.

* `grep` - search for patterns in a file.

* `awk/sed` - These are two very powerful commands that permit you to find specific strings in files and change them.


---

## A deeper dive

* The shell you use helps to organize and facilitate your interactions with the operating system

* `echo $SHELL` will display the shell you are using

* many software tools use hidden files and directories to provide customization e.g. `.gitconfig` or `.ssh`

* most shells have two files that are commonly used to provide user level customization `.zsh`, and `.zprofile`

## Resources{.smaller}

[Software carpentry](https://software-carpentry.org/lessons/) has a number of completely open
learning resources of interest:

- [Unix shell](https://swcarpentry.github.io/shell-novice/)
- [Using git](https://swcarpentry.github.io/git-novice/)
- [Using R](https://swcarpentry.github.io/r-novice-inflammation/)

Some of these charge a fee to use the materials:

-   <https://www.codecademy.com/learn/learn-the-command-line>
-   <https://www.edx.org/course/introduction-linux-linuxfoundationx-lfs101x-1>
-   <https://www.coursera.org/learn/unix>

## Let's get learning

- witnessing effects of file compression and decompression
- "storage quotas" (or monitoring `df') to avoid crashing your machine
- pattern matching with regular expressions; searching collections of files
- setting and retrieving values of ENVIRONMENT VARIABLES
